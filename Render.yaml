oy to Render" button
```yaml
services:
  - type: web
    name: morde-killer-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SESSION_ID
        sync: false
      - key: OWNER_NUMBER
        sync: false
      - key: PREFIX
        defaultValue: "!"
```
